/*
Package proxy implements a minimal PostgreSQL proxy that sits between a client
and a PostgreSQL server.

It provides the foundational building blocks for building higher-level component such as:
- Connection pooling
- Session state tracking
- Transaction-aware routing
*/
package proxy
